name: auto_attendance_webapp
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

# Firebase
  firebase_core: ^3.15.2
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.5.2
  firebase_storage: ^12.3.8
  
# Image picker
  image_picker: ^1.0.7

# api 
  http: ^1.2.0

# UI
  cupertino_icons: ^1.0.2

#camera
  camera: ^0.10.5+5

#testing
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  
  # Testing packages - compatible with Firebase 3.x:
  mockito: ^5.4.4
  build_runner: ^2.4.6
  fake_cloud_firestore: ^3.0.1  # ← Updated version
  firebase_auth_mocks: ^0.14.1  # ← Updated version
  
flutter:
  uses-material-design: true

